const {
  SPOTIFY_CLIENT_ID,
  SPOTIFY_SECRET_ID,
  SPOTIFY_REFRESH_TOKEN,
} = process.env

export const clientId = SPOTIFY_CLIENT_ID
export const secretId = SPOTIFY_SECRET_ID
export const refreshToken = SPOTIFY_REFRESH_TOKEN
